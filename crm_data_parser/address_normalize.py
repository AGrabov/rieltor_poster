"""Нормалізація адреси для пошуку: RU→UA місто, зрізання типу вулиці, folding.

CRM зберігає частини адреси непослідовно (інколи російською). Реєстр кадастру
(zem.center) шукає лише за українським написанням і не толерує тип вулиці в
запиті. Ці хелпери приводять адресу до форми, придатної для пошуку, і дають
``fold_cyrillic`` для алфавіто-толерантного ПОРІВНЯННЯ кандидатів.
"""

from __future__ import annotations

import re

# ── Міста: російська → українська ─────────────────────────────────────────
# Засіяно містами, де реально працюють. Легко доповнюється.
_RU_UA_CITY_RAW: dict[str, str] = {
    "Киев": "Київ",
    "Харьков": "Харків",
    "Одесса": "Одеса",
    "Днепр": "Дніпро",
    "Днепропетровск": "Дніпро",
    "Львов": "Львів",
    "Запорожье": "Запоріжжя",
    "Винница": "Вінниця",
    "Николаев": "Миколаїв",
    "Кривой Рог": "Кривий Ріг",
    "Чернигов": "Чернігів",
    "Черкассы": "Черкаси",
    "Житомир": "Житомир",
    "Сумы": "Суми",
    "Хмельницкий": "Хмельницький",
    "Черновцы": "Чернівці",
    "Ровно": "Рівне",
    "Ивано-Франковск": "Івано-Франківськ",
    "Тернополь": "Тернопіль",
    "Луцк": "Луцьк",
    "Ужгород": "Ужгород",
    "Кропивницкий": "Кропивницький",
    "Полтава": "Полтава",
    "Бровары": "Бровари",
    "Борисполь": "Бориспіль",
    "Ирпень": "Ірпінь",
    "Буча": "Буча",
    "Белая Церковь": "Біла Церква",
    "Бердянск": "Бердянськ",
    "Мариуполь": "Маріуполь",
    "Краматорск": "Краматорськ",
}

# Префікс міста ("м.", "г.", "город", "місто") на початку рядка.
_CITY_PREFIX_RE = re.compile(
    r"^\s*(?:місто|город|м|г|с|смт|пгт)(?:\.\s*|\s+)",
    re.IGNORECASE,
)

# Тип вулиці (UA + RU). Повні слова потребують пробілу після; скорочення —
# крапки або пробілу, тож назви на кшталт «Правди»/«Шевченка» не зачіпаються.
_STREET_TYPE_RE = re.compile(
    r"^\s*(?:"
    r"(?:вулиця|проспект|провулок|переулок|бульвар|площа|площадь|шосе|шоссе|"
    r"набережна|набережная|тупик|узвіз|проїзд|проезд|мікрорайон|квартал|"
    r"дорога|алея|лінія|маршрут)\s+"
    r"|"
    r"(?:вул|ул|просп|пров|пер|пр-т|пр-кт|пр-д|пр|бульв|бул|б-р|пл|шос|ш|наб|туп|"
    r"мкрн|мкр|кв-л|дор|ал)"
    r"(?:\.\s*|\s+)"
    r")",
    re.IGNORECASE,
)

# Тип вулиці (UA + RU токени) → канонічна укр. форма. Для розрізнення
# однойменних вулиць різного типу (вул./пров./пл. Шевченка) при виборі кандидата.
_TYPE_CANON: dict[str, str] = {
    "вулиця": "вул",
    "вул": "вул",
    "улица": "вул",
    "ул": "вул",
    "провулок": "пров",
    "пров": "пров",
    "переулок": "пров",
    "пер": "пров",
    "проспект": "просп",
    "просп": "просп",
    "пр-т": "просп",
    "пр-кт": "просп",
    "пр": "просп",
    "бульвар": "бул",
    "бульв": "бул",
    "бул": "бул",
    "б-р": "бул",
    "площа": "пл",
    "пл": "пл",
    "площадь": "пл",
    "шосе": "шосе",
    "шос": "шосе",
    "ш": "шосе",
    "шоссе": "шосе",
    "набережна": "наб",
    "наб": "наб",
    "набережная": "наб",
    "тупик": "туп",
    "туп": "туп",
    "узвіз": "узвіз",
    "проїзд": "проїзд",
    "пр-д": "проїзд",
    "проезд": "проїзд",
    "дорога": "дорога",
    "дор": "дорога",
    "алея": "алея",
    "ал": "алея",
    "мікрорайон": "мкр",
    "мкр": "мкр",
    "мкрн": "мкр",
    "квартал": "квартал",
    "кв-л": "квартал",
    "лінія": "лінія",
    "маршрут": "маршрут",
}
_TYPE_TOKEN_RE = re.compile(r"[А-Яа-яІіЇїЄєҐґ\-]+")

# Префікс номера будинку ("Будинок 6"/"буд. 6"/"корпус 6"/"№6" → "6").
_HOUSE_PREFIX_RE = re.compile(
    r"^\s*(?:№\s*|(?:будинок|буд|дом|корпус|корп)\.?\s+)",
    re.IGNORECASE,
)
# Хвіст-кваліфікатор будинку ("5 корпус 2" → "5"): корпус не входить у № ділянки.
_HOUSE_TAIL_RE = re.compile(r"[\s,]*(?:корпус|корп)\.?.*$", re.IGNORECASE)

# Folding для ПОРІВНЯННЯ (не для відображення): звести RU/UA-специфічні літери.
_FOLD_MAP = str.maketrans(
    {
        "і": "и",
        "ї": "и",
        "ы": "и",
        "є": "е",
        "э": "е",
        "ё": "е",
        "ґ": "г",
        "ь": "",
        "ъ": "",
        "'": "",
        "’": "",
        "ʼ": "",
        "`": "",
    }
)


def _build_city_index() -> dict[str, str]:
    """folded(рос. назва) → укр. назва."""
    return {fold_cyrillic(ru): ua for ru, ua in _RU_UA_CITY_RAW.items()}


def fold_cyrillic(s: str) -> str:
    """Звести рядок до форми для порівняння кандидатів (не для відображення)."""
    return s.strip().lower().translate(_FOLD_MAP)


def normalize_city(city: str) -> str:
    """Привести місто до українського написання.

    Зрізає префікс ('м.', 'г.', 'город', 'місто'), шукає у словнику RU→UA;
    якщо не знайдено — повертає рядок без префікса (як є).
    """
    stripped = _CITY_PREFIX_RE.sub("", city).strip()
    return _CITY_INDEX.get(fold_cyrillic(stripped), stripped)


def strip_street_type(street: str) -> str:
    """Зрізати тип вулиці ('вул.'/'ул.', 'пров.'/'пер.', 'просп.' тощо)."""
    return _STREET_TYPE_RE.sub("", street).strip()


def normalize_house(house: str) -> str:
    """Канонічна форма номера будинку для порівняння.

    Дзеркалить site-логіку ``normHouse`` (autocomplete.py): прибирає пробіли
    й дефіси, lower. Додатково зрізає префікс «Будинок/буд.». Тож
    ``19А`` = ``19-а`` = ``19 а`` = ``Будинок 19а``.
    """
    h = _HOUSE_PREFIX_RE.sub("", house or "")
    h = _HOUSE_TAIL_RE.sub("", h)
    return re.sub(r"[\s\-]+", "", h).lower()


# Канонічний тип → повне слово для відображення (лише розрізняльні, без «вул»).
_TYPE_CANON_DISPLAY: dict[str, str] = {
    "пров": "провулок",
    "просп": "проспект",
    "бул": "бульвар",
    "пл": "площа",
    "шосе": "шосе",
    "наб": "набережна",
    "туп": "тупик",
    "узвіз": "узвіз",
    "проїзд": "проїзд",
    "дорога": "дорога",
    "алея": "алея",
}

# Токени типів для пошуку в описі (розрізняльні; без однобуквених неоднозначних «ш»/«пр»).
_RECOVER_TYPE_TOKENS = [
    "шосе",
    "шоссе",
    "проспект",
    "просп",
    "пр-т",
    "пр-кт",
    "бульвар",
    "бульв",
    "бул",
    "б-р",
    "провулок",
    "пров",
    "переулок",
    "пер",
    "площа",
    "площадь",
    "пл",
    "набережна",
    "набережная",
    "наб",
    "тупик",
    "туп",
    "узвіз",
    "проїзд",
    "проезд",
    "дорога",
    "алея",
]
_RECOVER_TYPE_ALT = "|".join(re.escape(t) for t in sorted(_RECOVER_TYPE_TOKENS, key=len, reverse=True))
_NOT_CYR_LAT = r"(?![А-Яа-яІіЇїЄєҐґA-Za-z])"
_NOT_CYR_LAT_BEHIND = r"(?<![А-Яа-яІіЇїЄєҐґA-Za-z])"


def recover_street_type(street: str, *texts: str) -> str:
    """Якщо у вулиці немає типу — знайти розрізняльний тип у тексті(ах) поряд з назвою.

    Напр. вулиця="Харківське", опис містить "Харківське шосе" → "Харківське шосе".
    Типовий «вул» НЕ відновлюємо (він неявний на сайті). Шукаємо тип лише
    впритул до назви (через пробіл/кому), щоб не схопити чужий тип із сусіднього
    речення. Повертає "Назва Тип" або вихідну вулицю без змін.
    """
    if not street or street_type_canon(street):
        return street
    name = strip_street_type(street).strip()
    if len(name) < 3:
        return street
    name_re = re.escape(name)
    after = re.compile(
        _NOT_CYR_LAT_BEHIND + name_re + r"[\s,]*(" + _RECOVER_TYPE_ALT + r")" + _NOT_CYR_LAT, re.IGNORECASE
    )
    before = re.compile(
        _NOT_CYR_LAT_BEHIND + r"(" + _RECOVER_TYPE_ALT + r")\.?\s+" + name_re + _NOT_CYR_LAT, re.IGNORECASE
    )
    for text in texts:
        if not text:
            continue
        m = after.search(text) or before.search(text)
        if m:
            disp = _TYPE_CANON_DISPLAY.get(street_type_canon(m.group(1)))
            if disp:
                return f"{name} {disp}"
    return street


# ── RU→UA транслітерація назви вулиці ─────────────────────────────────────
# zem.center шукає лише за українським написанням (рос. → 0 результатів). CRM
# часто зберігає вулицю російською ("Якубенковская", "Дегтяревская"). Нижче —
# best-effort транслітерація: системні закінчення прикметників + букви. Стемові
# чергування (о/е→і всередині кореня) частково покриті закінченнями; решта —
# лексичні (Шёлковичная→Шовковична) — не передаються правилами. Це лише ДОДАТКОВИЙ
# варіант запиту: невдала транслітерація просто не дасть збігу (верифікація суворо
# звіряє номер будинку й усі слова назви), тож хибних заповнень вона не створює.

# Закінчення прикметників → варіанти укр. написання (довші суфікси — раніше).
# Деякі неоднозначні: "-овская" може бути -ів- (Якубенків-ська) АБО стем+ська
# (Москов-ська). Даємо обидва — пошук (zem) + верифікація відберуть правильний.
_RU_UA_SUFFIX_VARIANTS: tuple[tuple[str, tuple[str, ...]], ...] = (
    ("овская", ("івська", "овська")),
    ("ёвская", ("івська", "ьовська")),
    ("евская", ("івська", "евська")),
    ("инская", ("инська",)),
    ("ынская", ("инська",)),
    ("цкая", ("цька",)),
    ("ская", ("ська",)),
    ("овский", ("івський", "овський")),
    ("евский", ("івський", "евський")),
    ("ёвский", ("івський",)),
    ("цкий", ("цький",)),
    ("ский", ("ський",)),
    ("ской", ("ської",)),
    ("ная", ("на",)),
    ("ный", ("ний",)),
    ("ний", ("ний",)),
    ("ая", ("а",)),
    ("ое", ("е",)),
    ("ый", ("ий",)),
    ("ий", ("ий",)),
    ("ой", ("ий",)),
)
# Побуквений мапінг рос.-специфічних літер (поза закінченням).
_RU_LETTER_MAP = str.maketrans({"ы": "и", "э": "е", "ё": "о", "ъ": ""})


def _finish_token(stem_repl: str, upper: bool) -> str:
    low = stem_repl.translate(_RU_LETTER_MAP)
    if low.startswith("и"):  # рос. И- на початку → укр. І- (Ирпенская→Ірпін…)
        low = "і" + low[1:]
    return low.capitalize() if upper else low


def _translit_ru_token_variants(token: str) -> list[str]:
    """Best-effort укр. форми одного рос. токена (1–2 варіанти)."""
    low = token.lower()
    upper = token[:1].isupper()
    for suf, repls in _RU_UA_SUFFIX_VARIANTS:
        if low.endswith(suf) and len(low) > len(suf):
            stem = low[: -len(suf)]
            out: list[str] = []
            for r in repls:
                v = _finish_token(stem + r, upper)
                if v not in out:
                    out.append(v)
            return out
    return [_finish_token(low, upper)]


def ru_to_ua_variants(name: str) -> list[str]:
    """Список best-effort укр. написань назви (для неоднозначних закінчень — кілька).

    Верифікація суворо звіряє результат, тож зайві варіанти безпечні: хибний
    просто не дасть збігу. Обмежено кількома, щоб не плодити запитів.
    """
    parts = re.split(r"(\s+)", name)
    combos: list[str] = [""]
    for part in parts:
        if not part.strip():
            combos = [c + part for c in combos]
            continue
        variants = _translit_ru_token_variants(part)
        combos = [c + v for c in combos for v in variants]
        if len(combos) > 4:  # обмежуємо комбінаторику
            combos = combos[:4]
    out: list[str] = []
    for c in combos:
        if c and c not in out:
            out.append(c)
    return out


def transliterate_ru_to_ua(name: str) -> str:
    """RU→UA транслітерація назви (перший/основний варіант). Див. ``ru_to_ua_variants``."""
    variants = ru_to_ua_variants(name)
    return variants[0] if variants else name


def looks_russian(name: str) -> bool:
    """Евристика: назва радше російська (варто спробувати транслітерацію)."""
    low = (name or "").lower()
    if any(c in low for c in "ыэёъ"):
        return True
    if any(c in low for c in "іїєґ"):  # вже українські літери — не чіпаємо
        return False
    return bool(re.search(r"(ая|ий|ый|ой|ое)(\b|$)", low))


def street_type_canon(text: str) -> str:
    """Канонічний тип вулиці (UA) з рядка або ``''``, якщо типу немає.

    Шукає перший відомий тип-токен (UA або RU) у тексті й приводить до
    канонічної укр. форми ('ул'/'вул' → 'вул', 'пер'/'пров' → 'пров').
    Працює і на CRM-вулиці ('пер. Шевченка'), і на адресі реєстру
    ('м.Київ, вулиця Львівська, 19').
    """
    for token in _TYPE_TOKEN_RE.findall(text.lower()):
        canon = _TYPE_CANON.get(token)
        if canon:
            return canon
    return ""


# Публічна назва (для читабельності імпортів) + внутрішній індекс.
RU_UA_CITY = _RU_UA_CITY_RAW
_CITY_INDEX = _build_city_index()
